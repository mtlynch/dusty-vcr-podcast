{
  "name": "dusty-vcr",
  "version": "1.0.0",
  "description": "mtlynch.io dev scripts",
  "scripts": {
    "check-format": "prettier --ignore-path .gitignore --check ./**/*.js ./**/*.json ./**/*.css ./**/*.html ./content",
    "format": "prettier --ignore-path .gitignore --write ./**/*.js ./**/*.json ./**/*.css ./**/*.html ./content"
  },
  "author": "Michael Lynch",
  "devDependencies": {
    "prettier": "2.7.1",
    "prettier-plugin-go-template": "0.0.13"
  }
}
